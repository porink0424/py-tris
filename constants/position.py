# 盤面の大きさ
BOARD_WIDTH = 10
BOARD_HEIGHT = 20

# windowの位置・サイズ
WINDOW_X = 0
WINDOW_Y = 0
WINDOW_WIDTH = 1795
WINDOW_HEIGHT = 1100

# 盤面の左上・右下のますの中心位置
TOP_LEFT_X = int((309/1795) * WINDOW_WIDTH)
TOP_LEFT_Y = int((240/1100) * WINDOW_HEIGHT)
TOP_RIGHT_X = int((609/1795) * WINDOW_WIDTH)
TOP_RIGHT_Y = int((908/1100) * WINDOW_HEIGHT)

# 各マスの大きさ
recWidth = (TOP_RIGHT_X - TOP_LEFT_X) / (BOARD_WIDTH - 1)
recHeight = (TOP_RIGHT_Y - TOP_LEFT_Y) / (BOARD_HEIGHT - 1)

# NEXTミノ以降のミノが表示されている位置のx座標と、y座標の範囲
NEXT_MINOS_X = int((712/1795) * WINDOW_WIDTH)
NEXT_MINOS_Y_RANGES = [
    (int((256/1100) * WINDOW_HEIGHT),int((305/1100) * WINDOW_HEIGHT)),
    (int((343/1100) * WINDOW_HEIGHT),int((403/1100) * WINDOW_HEIGHT)),
    (int((437/1100) * WINDOW_HEIGHT),int((497/1100) * WINDOW_HEIGHT)),
    (int((535/1100) * WINDOW_HEIGHT),int((595/1100) * WINDOW_HEIGHT)),
    (int((632/1100) * WINDOW_HEIGHT),int((692/1100) * WINDOW_HEIGHT)),
]
NEXT_MINOS_BOX_X = int((657/1795) * WINDOW_WIDTH)

# HOLDミノが表示されている位置のx座標と、y座標の範囲
HOLD_MINO_X = int((270/1795) * WINDOW_WIDTH)
HOLD_MINO_Y_RANGE = (int((268/1100) * WINDOW_HEIGHT),int((331/1100) * WINDOW_HEIGHT))
HOLD_MINO_BOX_X = int((169/1795) * WINDOW_WIDTH)